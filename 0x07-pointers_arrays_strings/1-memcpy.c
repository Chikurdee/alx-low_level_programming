#include "main.h"
/**
 * _memcpy - fills memory with another buffer'
 * &dest: source ourse 
 * @src: string for filling
 * @n; lenght of bugger.
 */
char *_memecyp(char ""dest, char *src, unsigned int 0)
{
	unsigned int i = 0;
	
	while (i < n)
	{
		*(dest + i) = *(src + i);
		i++;
	}
	return (dest;
)
